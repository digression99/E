# 01.15, Quiz01
## E조 : 김도현, 주준영

## 1.

* Git clone : 원격에 있는 git tree를 로컬로 복사해서 가져오는 것 
* Git add : 수정 사항을 stage에 반영
* Git commit : stage에 반영된 것을 로컬 저장소에 저장하는 것 
* Git push : 로컬에서 발생한 commit 을 원격에 반영하는 것 
* Git fetch : 원격 저장소의 commit을 로컬 저장소로 가져오는 것
* Git pull : fetch + merge, 원격 저장소의 변경 내용을 working tree에 가져오고 merge하는것
* Git checkout : head를 해당 brach로 옮기는 것. Commit 객체의 내용을 working directory(wd)로 가져오는 것. 
* Git merge : 다른 가지에 있는 변경 내용을 현재 가지, master branch에 병합하는 것. 
* Git rebase : merge와 비슷한 기능을 가지는 명령 단, rebase에서의 병합은 하나의 가지로 합쳐진다.
* Git reset : 이전 commit 내용을 취소하는 것; 되돌리기. 

## 2. 
100 바이트

## 3. 

Git rebase -i 
후에 안에서 순서를 바꾼다. 

( * rebase 는 내 것을 가져다가 상대에 엎는 것이라면, rebase -i는 내 마음대로 고치겠다는 의미. )

## 4. 
check-sum 용량이 40바이트이므로 40x1000 = 40000바이트이다.
